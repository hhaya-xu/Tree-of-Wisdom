---
title: 收拾桌子-2026-06-01
date: 2026-06-01
---

# 2026-06-01 收拾桌子

## 本次做了什么

- 文姬 v5 左栏 CSS 上线（7变量体系 + docs-page 模式：浏览标题/文件夹底边线+圆角+hover/子条目引导线/滚动条）
- 修复 folder-count JS 注入（去重两份注入代码）
- 删除面包屑导航、恢复 ContentMeta 日期+阅读时间
- folder-icon 三角箭头 18→14→11→9px（踩坑：CSS 出现两次，属性顺序不同导致正则漏改；SVG 模板重复 width 属性）
- 建立 preview-base.html 可视化预览体系（从 Quartz 构建产物生成，硬编码 Explorer 导航，DOM 标签级验证）
- 建立三层工作流（主公审查→文姬交付→伯喈部署）
- custom.scss 预埋 8 个 @zone 锚点
- 撰写 Quartz 全量踩坑经验帖（11 章）
- 与文姬通信对齐协作规范

## 决策

- **preview-base 替代文姬自抓**：文姬抓线上 HTML 因 JS 渲染拿不到 Explorer → 改为伯喈从本地构建产物生成含硬编码导航的独立预览文件
- **@zone 锚点体系**：不再靠字符串匹配替换 CSS，用 8 个 zone 标记精确定位
- **CSS 替换策略**：整块匹配不可靠（属性顺序不同会漏）→ 改为逐属性独立替换
- **folder-icon 尺寸**：18→14→11→9px，最终 9px 平衡视觉

## 新经验

- ✅ 成功模式：Quartz 构建产物的 body 直接复用 + 仅替换 Explorer 区 → DOM 100% 一致
- ✅ 成功模式：标签序列对比法（提取所有 tag.class#id 序列 diff）验证 DOM 一致性
- ✅ 成功模式：独立属性替换（`/width: 14px !important;/g` → `width: 9px !important;`）比整块替换可靠
- ⚠️ 教训：SVG 模板中重复 width 属性，HTML 取第一个 → 必须去重
- ⚠️ 教训：custom.scss 中同名选择器块出现两次（属性顺序不同），正则整块匹配只命中一处
- ⚠️ 教训：PowerShell Set-Content 破坏 UTF-8 → 所有文件写入只用 Node.js
- ⚠️ 教训：preview 缺 .page-header 包裹层导致 Grid 布局失败 → body 结构必须原样复用

## 自检

- [x] 有没有该登记但漏掉的？→ 已登记 Quartz 全量踩坑经验帖
- [x] 有没有遗留待办需要移交？→ 无，文姬待审查 preview-base
- [x] 有没有可以写入规则/模板的经验？→ 已写入 Quartz 经验帖 + @zone 锚点体系

## 待办

- [ ] 文姬验收 preview-base.html，开始 v6 CSS 设计
- [ ] 线上站 folder-icon 9px 效果确认
- [ ] 与玄策同步三层工作流架构

---

伯喈
